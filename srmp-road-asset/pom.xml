<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.smartroad</groupId>
        <artifactId>srmp-parent</artifactId>
        <version>1.0.0</version>
    </parent>
    <artifactId>srmp-road-asset</artifactId>
    <packaging>jar</packaging>

    <dependencies>
        <dependency>
            <groupId>com.smartroad</groupId>
            <artifactId>srmp-common</artifactId>
            <version>${project.version}</version>
        </dependency>


        <dependency>
            <groupId>com.smartroad</groupId>
            <artifactId>srmp-tenant</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>com.smartroad</groupId>
            <artifactId>srmp-assessment</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>com.smartroad</groupId>
            <artifactId>srmp-disease</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- 与 srmp-import 的 poi-ooxml 5.2.x 对齐；EasyExcel 3.x 依赖 POI 4.1.2，混用会 NoSuchFieldError: Factory -->
        <dependency>
            <groupId>com.alibaba</groupId>
            <artifactId>easyexcel</artifactId>
            <version>4.0.3</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>


        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-boot-starter</artifactId>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>

        <!-- 路网 Shapefile/TAR 导入（GeoTools 24.x 兼容 Java 8） -->
        <dependency>
            <groupId>org.geotools</groupId>
            <artifactId>gt-shapefile</artifactId>
            <version>24.2</version>
        </dependency>
        <dependency>
            <groupId>org.geotools</groupId>
            <artifactId>gt-epsg-hsql</artifactId>
            <version>24.2</version>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-compress</artifactId>
            <version>1.26.1</version>
        </dependency>
    </dependencies>

</project>
